[package]
name = "Rustic64Shell"
version = "0.1.1"
edition = "2024"
authors = ["safedv"]

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
codegen-units = 1